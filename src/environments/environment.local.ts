export const environment = {
  production: false,
  developer: true,
  name: 'local',
  apiBaseUrl: 'https://localhost:44350/api/v1',
  appVersion: require('../../package.json').version + '-local',
};
